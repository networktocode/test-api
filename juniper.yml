---

  - name: get facts
    hosts: all
    connection: local
    gather_facts: no

    tasks:

      - junos_get_facts:
          host: "{{ inventory_hostname }}"
          user: "ntc"
          passwd: "ntc123"