---

  - name: get facts
    hosts: all
    connection: local
    gather_facts: no

    tasks:

      - eos_facts:
          host: "{{ ip_info[inventory_hostname] }}"
          username: "ntc"
          password: "ntc123"
          transport: "cli"

