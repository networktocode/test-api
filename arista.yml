---

  - name: get facts
    hosts: all
    connection: local
    gather_facts: no

    tasks:

      - eos_facts:
          host: "{{ inventory_hostname }}"
          username: "ntc"
          password: "ntc123"
          transport: "cli"

